#!/bin/bash

# Get the current branch name
branch="$(git rev-parse --abbrev-ref HEAD)"

if [[ "$branch" == "main" ]]; then
  echo "Direct commit to main is not allowed. Please commit to a feature branch and open a PR."
  exit 1
fi

exit 0
